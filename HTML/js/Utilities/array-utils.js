function ShuffleFisherYates(list){for(var i=list.length;i>1;){var index=Math.floor(Math.random()*i);i--;var value=list[index];list[index]=list[i],list[i]=value}return list}function MaxElement(arr,func){if(arr&&arr.length>0){let max=arr[0],mapped=arr.map(func),maxV=mapped[0];for(let i=1;i<arr.length;i++)mapped[i]>maxV&&(maxV=mapped[i],max=arr[i]);return max}return null}function MinElement(arr,func){if(arr&&arr.length>0){let min=arr[0],mapped=arr.map(func),minV=mapped[0];for(let i=1;i<arr.length;i++)mapped[i]<minV&&(minV=mapped[i],min=arr[i]);return min}return null}function FilterOnlyUnique(value,index,array){return array.indexOf(value)===index}